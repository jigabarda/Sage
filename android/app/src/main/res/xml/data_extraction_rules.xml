<?xml version="1.0" encoding="utf-8"?>
<!--
  Health data does not leave the device, and that includes leaving it sideways.

  Android 12+ splits backup into cloud backup and device-to-device transfer, and
  allowBackup="false" alone only covers the first. Both are excluded here so the
  episode log, the daily log and the medication list cannot be copied off by a
  vendor transfer tool or a Google account backup. There is nothing on the
  device worth restoring that is worth that exposure.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
